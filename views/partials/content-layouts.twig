<section class="flex-patterns">
{% for layout in layouts %}
    <div class="pattern pattern--{{ layout.acf_fc_layout }}">

        {% include 'partials/content-layouts/' ~ layout.acf_fc_layout ~ '.twig'
            ignore missing
            with { layout : layout } only
        %}

    </div>
{% endfor %}
</section>
