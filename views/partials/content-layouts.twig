
{% for layout in layouts %}

    {% include 'partials/content-layouts/' ~ layout.acf_fc_layout ~ '.twig'
        ignore missing
        with { layout : layout, parent_loop : loop } only
    %}

{% endfor %}
