{% extends 'layouts/main.twig' %}

{% block main_content %}
	{% for post in posts %}
		<article class="{{post.class}}">
			<h1>{{ post.title }}</h1>

			<h2>front-page.twig</h2>

			<div class="post-content">{{ post.content }}</div>

			<p>
				Using Timber version with Twig version: {{ constant('Twig_Environment::VERSION') }}</tt>
			</p>
		</article>
	{% endfor %}
{% endblock %}
